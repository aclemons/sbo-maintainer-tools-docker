name: Docker build 15.0

on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths:
      - Dockerfile
      - build_sbo-maintainer-tools.sh

concurrency:
  group: docker-15.0-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: false

env:
  REGISTRY_IMAGE: aclemons/sbo-maintainer-tools
  # renovate: datasource=github-releases depName=docker/buildx
  BUILDX_VERSION: 'v0.35.0'

jobs:
  docker:
    runs-on: ubuntu-24.04
    permissions:
      packages: write
    environment:
      name: dockerhub
      deployment: false
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          show-progress: false
          persist-credentials: false

      - name: Configure sbo-maintainer-tools version
        run: |
          grep ^SBO_MAINTAINER_TOOLS_VERSION build_sbo-maintainer-tools.sh  | sed 's/"//g' >> "$GITHUB_ENV"

      - name: Configure binfmt image
        run: |
          BINFMT_IMAGE="$(awk '/^FROM / {print $2}' binfmt/Dockerfile)"
          printf "BINFMT_IMAGE=%s\n" "$BINFMT_IMAGE" >> "$GITHUB_ENV"

      - name: Configure buildkit image
        run: |
          BUILDKIT_IMAGE="$(awk '/^FROM / {print $2}' buildkit/Dockerfile)"
          printf "BUILDKIT_IMAGE=%s\n" "$BUILDKIT_IMAGE" >> "$GITHUB_ENV"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@96fe6ef7f33517b61c61be40b68a1882f3264fb8 # v4.2.0
        with:
          image: ${{ env.BINFMT_IMAGE }}

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0 # zizmor: ignore[cache-poisoning] trusted release workflow
        with:
          version: ${{ env.BUILDX_VERSION }}
          driver-opts: image=${{ env.BUILDKIT_IMAGE }}

      - name: Login to DockerHub
        uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0 # v4.4.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Configure immutable tag fields
        run: |
          BUILD_DATE="$(date -u +%Y%m%d)"
          BUILD_RUN="$(printf '%06d' "${GITHUB_RUN_NUMBER}")"
          BUILD_SHA="${GITHUB_SHA::12}"
          printf "IMMUTABLE_TAG=%s-15.0-%s-%s-sha%s\n" \
            "$SBO_MAINTAINER_TOOLS_VERSION" \
            "$BUILD_DATE" \
            "$BUILD_RUN" \
            "$BUILD_SHA" >> "$GITHUB_ENV"

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6.2.0
        with:
          images: |
            ${{ env.REGISTRY_IMAGE }}
          tags: |
            type=raw,value=latest
            type=raw,value=${{ env.SBO_MAINTAINER_TOOLS_VERSION }}
            type=raw,value=latest-15.0
            type=raw,value=${{ env.SBO_MAINTAINER_TOOLS_VERSION }}-15.0
            type=raw,value=${{ env.IMMUTABLE_TAG }}

      - name: Build and push
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          pull: true
          push: true
          platforms: linux/386,linux/amd64,linux/arm/v7
          cache-from: type=gha
          cache-to: type=gha,mode=max
          context: .
          file: Dockerfile
          labels: ${{ steps.meta.outputs.labels }}
          tags: ${{ steps.meta.outputs.tags }}
